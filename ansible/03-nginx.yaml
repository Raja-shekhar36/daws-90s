- name: install and start nginx
  hosts: frontend
  become: yes
  tasks:
  - name: istall nginx
    ansible.builtin.package:
      name: nginx
      state: present

  - name: start nginx
    service:
      name: nginx
      state: started
      enabled: yes