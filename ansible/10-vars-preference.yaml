- name: variables preference
  hosts: frontend
  vars: 
    full_name: "Raj from play"
  # vars_files:
  # - course.yaml
  vars_prompt:
  - name: full_name
    prompt: "please enter your full_name"
    private: false
  tasks: 
  - name: print vars preference
    vars:
     # full_name: "Raj from TASK level"
    debug: 
      msg: "Hi, {{ full_name }}"