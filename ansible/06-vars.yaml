- name: demo on vars files
  hosts: localhost
  connection: local
  vars_files:
  - course.yaml  
  tasks: 
  - name: print varibale informaion
    debug:
      msg: "Hi, i am learning {{ COURSE }}, Trainer is {{ TRAINER }}, and Duration is {{ DURATION }}"
